#include <iostream>

using namespace std;


int Max(int x, int y)
{
	return x >y ? x:y;
}

float Max(float x, float y)
{
	return x >y ? x:y;
}

int main()
{
	auto num = Max(3,8);
	cout<<num<<endl;
	auto num2 = Max(4.6 , 2.3);
	cout<<num2<<endl;
	return 0;
}
