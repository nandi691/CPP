#ifndef CAR_H
#define CAR_H

class Car{
private:
	float fuel;
	float speed;
	int passengers;
public:
	Car();
	~Car();
	Car(float amount); //Parameterized constructor
	void FillFuel(float amount);
	void Accelerate();
	void Brake();
	void AddPassengers(int count);
	void DashBoard();
};

#endif
